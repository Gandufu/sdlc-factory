package dev.sdlc.factory.contracts.context;

/** 上下文来源类别（context-manifest.schema.json: source_class）。 */
public enum SourceClass {
    BASELINE,
    REFERENCE,
    RULE,
    PROMPT,
    CAPABILITY,
    FEEDBACK
}
