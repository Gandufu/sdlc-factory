package dev.sdlc.factory.contracts.run;

/** Run 所属阶段（run-request.schema.json: stage）。 */
public enum RunStage {
    INITIALIZATION,
    REQUIREMENT,
    DESIGN,
    CODING,
    TESTING,
    SYSTEM_ACCEPTANCE
}
